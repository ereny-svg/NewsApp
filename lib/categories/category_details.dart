import 'package:flutter/material.dart';

class CategoryDetails extends StatelessWidget {
  String categoryId='';
CategoryDetails({required this.categoryId});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('cccccc'));
  }
}